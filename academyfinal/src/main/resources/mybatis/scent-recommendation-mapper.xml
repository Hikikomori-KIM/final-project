<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="ScentRecommendation">
  <!-- 인서트 -->
  <insert id="add">
  	insert into scent_recommendation (scent_recommendation_no, product_no,
  	scent_recommendation_comment, top_score_type)
  	values(#{scentRecommendationNo}, #{productNo}, #{scentRecommendationComment}, #{topScoreType})
  </insert>
  
  <!-- 시퀀스 -->
	<select id="sequence" resultType="int">
		select scent_Recommendation_seq.nextval from dual
	</select>
  </mapper>
  