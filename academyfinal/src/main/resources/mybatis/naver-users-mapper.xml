<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="naverUsers">
 
 	<insert id="join">
 		insert into naver_users(naver_users_id, naver_users_name, naver_users_email, naver_users_mobile)
 		values(#{naverUsersId}, #{naverUsersName}, #{naverUsersEmail}, #{naverUsersMobile})
 	</insert>
 	
 	<select id="find" resultType="naverUsersDto">
 		select * from naver_users where naver_users_id = #{naverUsersId}
 	</select>
 	
 	
 	
  
  
  
  </mapper>